name: Release

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to release, for example 0.3.0 (or v0.3.0)
        required: true
        type: string
      build_number:
        description: Build number (versionCode) to use. Leave empty to auto-increment.
        required: false
        type: string

concurrency:
  group: pubspec-build-bump-${{ github.event.repository.default_branch }}
  cancel-in-progress: false

permissions:
  contents: write
  actions: read
  pull-requests: read

jobs:
  bump-build-number:
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.bump.outputs.VERSION_NAME }}
      build_number: ${{ steps.bump.outputs.BUILD_NUMBER }}

    steps:
      - name: Checkout default branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Set version and build number
        id: bump
        shell: bash
        run: |
          set -euo pipefail

          VERSION_INPUT="${{ github.event.inputs.version }}"
          VERSION_NAME="${VERSION_INPUT#v}"
          if ! [[ "$VERSION_NAME" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid version: $VERSION_INPUT" >&2
            exit 1
          fi

          VERSION_LINE="$(grep -E '^version:' pubspec.yaml | awk '{print $2}')"
          if [ -z "$VERSION_LINE" ]; then
            echo "Failed to read version from pubspec.yaml" >&2
            exit 1
          fi

          CURRENT_BUILD_NUMBER="${VERSION_LINE##*+}"
          if ! [[ "$CURRENT_BUILD_NUMBER" =~ ^[0-9]+$ ]]; then
            echo "Invalid build number in pubspec.yaml version: $VERSION_LINE" >&2
            exit 1
          fi

          MANUAL_BUILD_NUMBER="${{ github.event.inputs.build_number || '' }}"
          if [ -n "$MANUAL_BUILD_NUMBER" ]; then
            if ! [[ "$MANUAL_BUILD_NUMBER" =~ ^[0-9]+$ ]]; then
              echo "Invalid build number input: $MANUAL_BUILD_NUMBER" >&2
              exit 1
            fi
            NEXT_BUILD_NUMBER="$MANUAL_BUILD_NUMBER"
          else
            NEXT_BUILD_NUMBER=$((CURRENT_BUILD_NUMBER + 1))
          fi

          NEXT_VERSION="${VERSION_NAME}+${NEXT_BUILD_NUMBER}"
          if [ "$NEXT_VERSION" != "$VERSION_LINE" ]; then
            sed -i "s/^version: .*/version: ${NEXT_VERSION}/" pubspec.yaml

            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add pubspec.yaml
            git commit -m "chore: Release ${VERSION_NAME} (build ${NEXT_BUILD_NUMBER})"
            git push origin "HEAD:${{ github.event.repository.default_branch }}"
          else
            echo "Version unchanged: ${NEXT_VERSION}"
          fi

          echo "VERSION_NAME=${VERSION_NAME}" >> "$GITHUB_OUTPUT"
          echo "BUILD_NUMBER=${NEXT_BUILD_NUMBER}" >> "$GITHUB_OUTPUT"

  create-release:
    needs: bump-build-number
    runs-on: ubuntu-latest
    outputs:
      version: v${{ needs.bump-build-number.outputs.version_name }}
      version_number: ${{ needs.bump-build-number.outputs.version_name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Prepare release notes
        shell: bash
        run: |
          set -euo pipefail

          if [ -s CHANGELOG.md ]; then
            cp CHANGELOG.md release-notes.md
          else
            echo "Release v${{ needs.bump-build-number.outputs.version_name }}" > release-notes.md
          fi

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          tag: v${{ needs.bump-build-number.outputs.version_name }}
          name: v${{ needs.bump-build-number.outputs.version_name }}
          bodyFile: release-notes.md
          draft: false
          prerelease: ${{ contains(needs.bump-build-number.outputs.version_name, 'beta') || contains(needs.bump-build-number.outputs.version_name, 'alpha') || contains(needs.bump-build-number.outputs.version_name, 'rc') }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify release created
        run: |
          echo "Successfully created release v${{ needs.bump-build-number.outputs.version_name }}"
          echo "Release notes sourced from CHANGELOG.md when available"
          echo "Release URL: https://github.com/${{ github.repository }}/releases/tag/v${{ needs.bump-build-number.outputs.version_name }}"

  build-and-upload:
    needs:
      - bump-build-number
      - create-release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Compute build metadata
        id: build_meta
        shell: bash
        run: |
          set -euo pipefail
          SHORT_SHA="$(git rev-parse --short HEAD)"
          echo "BUILD_NAME=${{ needs.create-release.outputs.version_number }}-${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "BUILD_NUMBER=${{ needs.bump-build-number.outputs.build_number }}" >> "$GITHUB_OUTPUT"

      - name: Setup Android signing
        shell: bash
        run: |
          set -euxo pipefail
          test -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}"
          test -n "${{ secrets.ANDROID_STORE_PASSWORD }}"
          test -n "${{ secrets.ANDROID_KEY_ALIAS }}"
          printf '%s' "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/upload-keystore.jks
          {
            echo "storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}"
            echo "keyPassword=${{ secrets.ANDROID_STORE_PASSWORD }}"
            echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}"
            echo "storeFile=../upload-keystore.jks"
          } > android/key.properties
          keytool -list -v \
            -keystore android/upload-keystore.jks \
            -storepass "${{ secrets.ANDROID_STORE_PASSWORD }}" \
            -alias "${{ secrets.ANDROID_KEY_ALIAS }}" > /dev/null

      - name: Setup Android SDK components
        shell: bash
        env:
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
        run: |
          set -euxo pipefail
          SDKMANAGER="$(command -v sdkmanager || true)"
          if [ -z "$SDKMANAGER" ]; then
            SDKMANAGER="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          fi

          yes | "$SDKMANAGER" --licenses || true
          "$SDKMANAGER" \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;35.0.0" \
            "cmake;3.22.1" \
            "ndk;28.2.13676358"

      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64 --build-name "${{ steps.build_meta.outputs.BUILD_NAME }}" --build-number "${{ steps.build_meta.outputs.BUILD_NUMBER }}" --obfuscate --split-debug-info=build/app/outputs/symbols --tree-shake-icons

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload release assets
        if: success()
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.create-release.outputs.version }}
          artifacts: build/app/outputs/flutter-apk/app-release.apk
          allowUpdates: true
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify build completion
        run: |
          echo "Successfully built and uploaded release assets"
          echo "Release URL: https://github.com/${{ github.repository }}/releases/tag/${{ needs.create-release.outputs.version }}"

  build-ios-ipa:
    needs:
      - bump-build-number
      - create-release
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Compute build metadata
        id: build_meta
        shell: bash
        run: |
          set -euo pipefail
          SHORT_SHA="$(git rev-parse --short HEAD)"
          echo "BUILD_NAME=${{ needs.create-release.outputs.version_number }}-${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "BUILD_NUMBER=${{ needs.bump-build-number.outputs.build_number }}" >> "$GITHUB_OUTPUT"

      - name: Import signing certificate
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.IOS_DISTRIBUTION_CERT_BASE64 }}
          p12-password: ${{ secrets.IOS_DISTRIBUTION_CERT_PASSWORD }}

      - name: Install provisioning profile
        shell: bash
        run: |
          set -euo pipefail
          PROFILE_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PROFILE_DIR"
          PROFILE_PATH="$RUNNER_TEMP/appstore.mobileprovision"
          printf '%s' "${{ secrets.IOS_APPSTORE_PROFILE_BASE64 }}" | base64 --decode > "$PROFILE_PATH"
          UUID=$(/usr/libexec/PlistBuddy -c 'Print UUID' /dev/stdin <<< "$(security cms -D -i "$PROFILE_PATH")")
          cp "$PROFILE_PATH" "$PROFILE_DIR/$UUID.mobileprovision"

      - name: Generate export options
        shell: bash
        run: |
          set -euo pipefail
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>teamID</key>
            <string>${{ secrets.IOS_TEAM_ID }}</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>com.stayup.schedule</key>
              <string>${{ secrets.IOS_APPSTORE_PROFILE_NAME }}</string>
            </dict>
          </dict>
          </plist>
          EOF

      - name: Build signed IPA
        run: flutter build ipa --release --build-name "${{ steps.build_meta.outputs.BUILD_NAME }}" --build-number "${{ steps.build_meta.outputs.BUILD_NUMBER }}" --export-options-plist=ios/ExportOptions.plist

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-ipa
          path: build/ios/ipa/*.ipa

      - name: Upload to TestFlight
        uses: apple-actions/upload-testflight-build@v3
        with:
          app-path: build/ios/ipa/*.ipa
          issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
          api-key-id: ${{ secrets.APPSTORE_API_KEY_ID }}
          api-private-key: ${{ secrets.APPSTORE_API_PRIVATE_KEY }}

  notify-completion:
    needs:
      - create-release
      - build-and-upload
      - build-ios-ipa
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Notify success
        if: needs.build-and-upload.result == 'success' && needs.build-ios-ipa.result == 'success'
        run: |
          echo "Release ${{ needs.create-release.outputs.version }} completed successfully"
          echo "All build artifacts have been uploaded"

      - name: Notify failure
        if: needs.build-and-upload.result == 'failure' || needs.build-ios-ipa.result == 'failure'
        run: |
          echo "Failed to complete release ${{ needs.create-release.outputs.version }}"
          echo "Please check the workflow logs for details"
